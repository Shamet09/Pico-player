# Documento di specifica originale: resta qui in locale come riferimento
# (la sezione 8 del README ne discute gli scostamenti) ma non va pubblicato.
# Nota: si ignora SOLO questo file, non tutti i .docx, altrimenti verrebbe
# esclusa anche GUIDA_DA_ZERO.docx, che invece fa parte del progetto.
PicoPlayer_Prompt_ClaudeCode.docx

# Artefatti di build
build/
test/build/

# Rigenerato ad ogni build da tools/build.sh, contiene percorsi assoluti
# della macchina su cui e' stato compilato: non va condiviso.
compile_commands.json
*.o
*.elf
*.bin
*.uf2
*.dis
*.map
__pycache__/
